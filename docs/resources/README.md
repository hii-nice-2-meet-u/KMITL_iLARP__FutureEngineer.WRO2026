# docs/resources

This directory is reserved for photographs, wiring diagrams, state-machine
figures, and CAD renders referenced by the documentation. No binary resource
files are included in the current source snapshot; links are added only when
the corresponding files are committed.
